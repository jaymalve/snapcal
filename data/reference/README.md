# Reference Assets

This directory stores repo-tracked reference files that are stable across machines:

- `food101_usda_mapping.csv`: seeded one-row-per-class nutrition mapping template
- `manifests/`: generated manifest outputs that use relative repo paths

`data/raw` and `data/processed` are intentionally gitignored because they contain local datasets and large preprocessing artifacts.
